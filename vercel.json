{
  "buildCommand": "npm run build && rm -rf public && cp -r dist/public public && echo '/*    /index.html   200' > public/_redirects",
  "outputDirectory": "public",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}